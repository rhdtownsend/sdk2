#!/bin/sh
#
# Launch an SDK2 docker session

### VALIDATION ###

# Check SDK2_ROOT is set and valid

if [[ -z $SDK2_ROOT ]]; then
    echo "Environment variable SDK2_ROOT is not set"
    exit 1
fi

if [[ ! -d $SDK2_ROOT ]]; then
    echo "Root directory '$SDK2_ROOT' is missing"
    exit 1
fi

# Launch the session

C_SDK2_ROOT=/root/sdk2

docker run -it --name sdk2_build --rm \
       --env SDK2_ROOT=$C_SDK2_ROOT --env SDK2_PROFILE --env SDK2_RELEASE \
       --mount type=bind,source=$SDK2_ROOT,target=$C_SDK2_ROOT \
       rhdtownsend/sdk2_build bash
