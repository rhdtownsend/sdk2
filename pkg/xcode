# SDK2 package definition for xcode (local install)

# Set package-specific variables

PKG_NAME=xcode
SRC_FILE=MacOSX${XCODE_SDK_VERSION}.sdk.tar.xz

SRC_DIR=$PKG_NAME
BUILD_DIR=$SRC_DIR

# Functions

build () {

    # Nothing to do

    exit 0

}
