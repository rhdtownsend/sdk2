#!/bin/sh
#
# Legacy link script for blas

if [ -n "@SDK_MATH_SLOT@" ]; then
    math_slot=@SDK_MATH_SLOT@
else
    math_slot=default
fi

pkgconf --define-variable=math_slot=${math_slot} --static --libs blas
