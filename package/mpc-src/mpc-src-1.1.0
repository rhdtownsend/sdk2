# SDK2 package definition for mpc-src

# Set package-specific variables

PACKAGE_NAME=mpc-src
SRC_FILE=mpc-1.1.0.tar.gz

SRC_DIR=$PACKAGE_NAME
BUILD_DIR=$SRC_DIR

# Functions

build () {

    echo "Building $PACKAGE_NAME in $BD"

    echo "Nothing to do..."

}

install () {

    echo "Installing $PACKAGE_NAME from $BD"

    echo "Nothing to do..."

}
