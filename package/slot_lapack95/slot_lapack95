# SDK2 package definition for slotted lapack95

# Set package-specific variables

PACKAGE_NAME=slot_lapack95

SRC_DIR=$PACKAGE_NAME
BUILD_DIR=$SRC_DIR

# Functions

build () {

    echo "Building $PACKAGE_NAME in $BD"

}    

install () {

    echo "Installing $PACKAGE_NAME from $BD"

    # Create the link script

    sed -e s/@SDK_ROOT@/\${${PROFILE_PREFIX}_ROOT}/g \
    	-e s/@SDK_MATH_SLOT@/\${${PROFILE_PREFIX}_MATH_SLOT}/g \
	${SDK2_PROFILE_ROOT}/common/slot_lapack95/lapack95_link > ${DEST_ROOT}/bin/lapack95_link
    chmod a+x ${DEST_ROOT}/bin/lapack95_link

}
