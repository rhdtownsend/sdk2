# SDK2 package definition for gmp-src

# Set package-specific variables

PACKAGE_NAME=gmp-src
SRC_FILE=gmp-6.2.1.tar.bz2

SRC_DIR=$PACKAGE_NAME
BUILD_DIR=$SRC_DIR

# Functions

build () {

    echo "Building $PACKAGE_NAME in $BD"

    echo "Nothing to do..."

}

install () {

    echo "Installing $PACKAGE_NAME from $BD"

    echo "Nothing to do..."

}
